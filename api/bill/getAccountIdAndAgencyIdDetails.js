'use strict';
var async = require('async');
module.exports = get;

function get(req, res) {
  var box = {
    req: req,
    reqParams: req.params,
    res: res,
    reqBody: req.body,
    resBody: {}
  };

  async.series([
    getAccountIdAndAgencyIdDetails.bind(null, box)
    ],
    function (err) {
      if (err)
        return respondWithError(res, err);
      sendJSONResponse(res, box.resBody);
    });
}

function getAccountIdAndAgencyIdDetails(box, nextFunc) {
  var whoAmI = getAccountIdAndAgencyIdDetails.name;
  var sql = "SELECT * FROM bill_details WHERE agency_id = '"+box.reqParams.agency_id+"' AND account_number = '"+box.reqParams.account_number+"'  ORDER BY bill_detail_id DESC ";
  con.query(sql,
  function (err, result) {
        if (err){
          console.info(err)
          console.info("Error In => "+whoAmI);
          return nextFunc(
            new ActErr(whoAmI, ActErr.DBOperationFailed, 'Get All Account Details By Agency Id', err)
            );
        }else{
          if(result.length >= 1){
            box.resBody.success = true;
            box.resBody.data = result;
          }else{
            box.resBody.success = false;
            box.resBody.message = "No Records Found !";
          }
        }
    return nextFunc();
  }
  );
}
